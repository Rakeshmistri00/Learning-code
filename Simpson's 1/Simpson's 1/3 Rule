#include<stdio.h>

float f(float x)
{
    return x*x;
}

int main()
{
    float a,b,h,x1,I;

    printf("Enter lower and upper limit: ");
    scanf("%f%f",&a,&b);

    h=(b-a)/2;

    x1=(a+b)/2;

    I=(h/3)*(f(a)+4*f(x1)+f(b));

    printf("Integral = %f",I);

    return 0;
}
