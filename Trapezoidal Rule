#include<stdio.h>

float f(float x)
{
    return x*x;
}

int main()
{
    float a,b,h,I;

    printf("Enter lower limit and upper limit: ");
    scanf("%f%f",&a,&b);

    h=b-a;

    I=(h/2)*(f(a)+f(b));

    printf("Integral = %f",I);

    return 0;
}
